<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="120dp"
    android:background="@color/color_scheme_pink"
    tools:padding="20dp"
    android:paddingBottom="20dp">
    <!--
    we can add the logo or something ?
    Text isn't really going well.
    <TextView
        android:text="WTM, MANIPAL"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:textAlignment="gravity"
        android:shadowColor="@color/color_scheme_blue"
        android:textAllCaps="false"
        android:padding="20dp"
        android:paddingBottom="20dp"
        android:typeface="sans"
        android:textSize="30sp"
        android:textStyle="bold"
        android:textColor="@color/color_scheme_white"
        android:textColorHighlight="@color/color_navigation_list_background"
        tools:textAlignment="center"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="19dp"
        android:layout_marginStart="19dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true" />
        -->
</RelativeLayout>